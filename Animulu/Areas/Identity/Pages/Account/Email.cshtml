@page "/account/email"
@model Animulu.Areas.Identity.Pages.Account.EmailModel
@{
    ViewData["Title"] = "Account email";
    ViewData["SubTitle"] = "Account";
    ViewData["Identity"] = "true";
}
<div class="row my-5">
    <div class="offset-xl-3 col-xl-6 offset-md-1 col-md-10 col-12 bg-grey-light border-radius-light py-4 mb-2 @if (Model.StatusMessage == "" || Model.StatusMessage == null) {<text>d-none</text>}">
        @Model.StatusMessage
    </div>
    <div class="row offset-xl-3 col-xl-6 offset-md-1 col-md-10 col-12 bg-grey border-radius-light py-4 mx-md-auto mx-0">
        <partial name="_LayoutAccountPartial" />
        <div class="col-md-8 col-12 bg-grey">
            <h5 class="mt-3">Email</h5>
            <form id="email-form" method="post" class="col-sm-7 col-12 px-0 mt-3 font-content">
                <span>Change email</span>
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group mb-3 mt-2">
                    <input asp-for="Email" placeholder="Email" class="form-control bg-grey-dark border-grey-dark form-dark-fix" />
                    <span asp-validation-for="Email" class="text-danger"></span>
                </div>
                <div class="form-group mb-3 mt-2">
                    <input asp-for="Input.NewEmail" placeholder="New Email" class="form-control bg-grey-dark border-grey-dark form-dark-fix" />
                    <span asp-validation-for="Input.NewEmail" class="text-danger"></span>
                </div>
                <div class="form-group my-3">
                    <input asp-for="Input.Password" placeholder="Password" class="form-control bg-grey-dark border-grey-dark form-dark-fix" />
                </div>
                <button id="change-email-button" type="submit" asp-page-handler="ChangeEmail" class="btn btn-form">Change</button>
            </form>
        </div>
    </div>
</div>